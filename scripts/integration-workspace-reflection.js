'use strict';
const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const root=path.resolve(__dirname,'..');
const app=fs.readFileSync(path.join(root,'src/app.js'),'utf8');
const html=fs.readFileSync(path.join(root,'src/index.html'),'utf8');
const css=fs.readFileSync(path.join(root,'src/styles.css'),'utf8');
assert.match(html,/data-testid="messages"/);
assert.match(html,/connected-detail-dialog/);
assert.match(app,/const PAGE_SIZE = 40/);
assert.match(app,/function renderPagination/);
assert.match(app,/function renderMessages/);
assert.match(app,/function agendaEvents/);
assert.match(app,/Website order/);
assert.match(app,/integrationRemote\('agenda'\)/);
assert.match(app,/message_response_strategy/);
assert.match(css,/scrollbar-gutter:stable/);
assert.match(css,/calendar-month/);
assert.match(css,/message-card/);
console.log('NEXA_WORKSPACE_REFLECTION_V1: pagination, scroll, agenda, contacts, leads, messages and AI context verified.');
