Nexa Smart Office Bot 1.1.0 validation report
------------------------------------------------

Scope
-----
Connected Business API-key integration, AutoMarket Pro resource discovery/cache,
permission-controlled Nexa Pulse notifications, Windows native alerts, animated
in-app notification assistant, quiet hours, system tray, and Windows startup.

Completed validation
--------------------
- All project JavaScript files passed `node --check`.
- `npm run validate`: PASS.
- Delivery validator: PASS.
- Internal project validator: PASS.
- Nexa contract gate: PASS.
- Core SQLite/CRUD/backup integration tests: 27 PASS.
- Connected-business/API/notification tests: 7 PASS.
- Implementation tests: PASS.
- Acceptance tests: PASS.
- `npm ci --ignore-scripts` installed 284 locked packages successfully.
- Package lock was sanitized of 284 environment-specific internal registry URLs.
- No API key, production database, customer data, node_modules, or build artifact is included.

Tested behaviors
----------------
- Migration 3 is idempotent.
- New integration and notification tables are created.
- Website URLs normalize to `/api/v1/index.php`.
- Bearer authentication is used without exposing the key publicly.
- `ping` and `connection-map` discovery works against a controlled API fixture.
- First synchronization creates a baseline without historical notification spam.
- A later new order creates one deduplicated notification.
- Notification permission is explicit.
- A native notification is delivered by the controlled Notification fixture.
- Integration status, snapshots, preferences, read state, dismissal, and dedupe persist.
- Installer, Portable, and ZIP targets remain declared.

External-stage validation
-------------------------
Electron UI smoke and Windows packaging remain mandatory in GitHub Actions. The
local container could install the complete package graph but could not download
the Electron runtime from its restricted CDN environment. The workflow will stop
before publishing if renderer startup, UI smoke, installer, portable, ZIP, or
installer/uninstaller validation fails.
